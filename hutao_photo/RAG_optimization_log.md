### 胡桃 RAG 参数调整记录

- **调整日期**：2026-08-01
- **调整参数**：`k` 值（检索返回的文档块数量）
- **调整前**：k=3
- **调整后**：k=2
- **调整原因**：k=3 时回答中混杂了多个文本块，导致内容发散；调整为 2 后回答更聚焦，信息冗余减少。
- **观察效果**：
  - 正面：回答更聚焦，发散性降低。
  - 负面：暂未发现关键信息丢失。
  - 对于需要多源信息佐证的复杂问题（如涉及两个不同角色），k=2 可能覆盖不足，后续需要针对性测试。
- **后续建议**：保持 k=2，若后期发现信息不足，可考虑将 `chunk_size` 从 500 调整为 400，在保持块数较少的同时提高块内信息密度。